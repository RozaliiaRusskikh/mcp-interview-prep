[project]
name = "mcp-interview-prep"
version = "0.1.0"
description = "MCP server that exposes Roza Russkikh's resume, skills, and STAR-format behavioral stories as tools/resources for AI-driven interview prep"
readme = "README.md"
authors = [
    { name = "Roza Russkikh", email = "rrrusskikh@gmail.com" },
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.28.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
