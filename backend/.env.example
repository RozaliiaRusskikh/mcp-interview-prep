GOOGLE_API_KEY=

# Optional overrides
# FRONTEND_ORIGIN=http://localhost:5173
# LOG_FILE=backend.log

# Optional: Langfuse tracing (backend/main.py, eval/run_eval.py). Without
# these, tracing/scoring calls no-op silently — nothing breaks, you just
# don't get a dashboard. Get values from your Langfuse project settings.
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com
